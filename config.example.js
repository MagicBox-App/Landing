/**
 * Configuración local de Gemini API
 * INSTRUCCIONES:
 * 1. Copia este archivo a "config.js" (sin el .example)
 * 2. Reemplaza la key con tu API key válida de Google Gemini
 * 3. NUNCA hagas commit de config.js — está en .gitignore
 */

// Tu API key de Google Gemini (obtén gratis en ai.google.dev)
window.GEMINI_API_KEY = 'TU_API_KEY_AQUI';
